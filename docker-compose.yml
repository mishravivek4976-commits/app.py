version: '3.8'

services:
  ai-doctor:
    build: .
    ports:
      - "8501:8501"
    environment:
      - HF_API_TOKEN=${HF_API_TOKEN}
      - STREAMLIT_SERVER_HEADLESS=true
    volumes:
      - ./:/app
    stdin_open: true
    tty: true
